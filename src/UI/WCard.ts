// import { Component } from "src/framework/framework";

// interface IProp {
// 	title: string;
// 	desc: string;
// 	descColor: string;
// }

// export default class WCard extends Component<any, IProp> {
// 	protected template(): string {
// 		const { title, desc, descColor = "var(--text-muted)" } = this.props;

// 		return `<div class="title">${title}</div>
// 			${desc ? `<div class="desc" style='color:${descColor}'>${desc}</div>` : ""}
//         `;
// 	}
// }
